---

---

<style is:global lang="scss">
  html.light {
    --forground-color-rgb: 33, 33, 33;
    --background-color-rgb: 255, 255, 255;
    --forground-color: rgb(--forground-color-rgb);
    --background-color: rgb(--background-color-rgb);
  }

  a {
    &:link {
      color: var(--foreground-color);
    }
    &:visited {
      color: var(--foreground-color);
    }
    &:hover {
      color: var(--foreground-color);
    }
    &:active {
      color: var(--foreground-color);
    }
  }
</style>
